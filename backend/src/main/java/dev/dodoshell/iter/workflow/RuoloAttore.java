package dev.dodoshell.iter.workflow;

// Relazione tra chi richiede la transizione e la richiesta specifica, non il ruolo globale dell'utente.
public enum RuoloAttore {
    PROPRIETARIO,
    RESPONSABILE
}
