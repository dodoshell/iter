server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router gestisce il routing lato client: qualunque percorso non
    # corrisponda a un file statico deve ricadere su index.html.
    location / {
        try_files $uri /index.html;
    }
}
